# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36
# Note: pgvector is a PostgreSQL extension used via raw SQL — the Python
# package is not required when using SQLAlchemy text() queries.

# Embeddings — CPU-only torch keeps the build under 500 MB.
# --extra-index-url adds the PyTorch index alongside PyPI (not instead of it).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
sentence-transformers==3.3.1
transformers==4.47.1

# OpenRouter (OpenAI-compatible API)
openai==1.59.6

# Validation & config
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
python-multipart==0.0.20

# Utilities
httpx==0.28.1
tenacity==9.0.0
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
